-- Love U 
-- [[ KHỞI TẠO BIẾN ]]
local lp = game:GetService("Players").LocalPlayer
local runService = game:GetService("RunService")
local uis = game:GetService("UserInputService")

-- Xóa UI cũ nếu đang chạy
local oldUI = game:GetService("CoreGui"):FindFirstChild("VuKhoa_Pro_Squircle")
if oldUI then oldUI:Destroy() end

-- Biến Logic Chức năng
local localMoveDir = Vector3.new(0,0,0)
local currentSpeed = 1 
local ghimActive, xrayActive, ghostActive, airWalkActive = false, false, false, false
local lockedPos, airWalkPart, ghostModel = nil, nil, nil

-- [[ HÀM NOTIFY ]]
local function CreateNotify(title, text, duration)
    local sg = Instance.new("ScreenGui", game.CoreGui)
    local mf = Instance.new("Frame", sg)
    mf.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    mf.Size = UDim2.new(0, 260, 0, 75)
    mf.Position = UDim2.new(1, 10, 1, -100)
    Instance.new("UICorner", mf).CornerRadius = UDim.new(0, 8)
    local s = Instance.new("UIStroke", mf)
    s.Color = Color3.fromRGB(255, 255, 0); s.Thickness = 2

    local tl = Instance.new("TextLabel", mf)
    tl.Text = title; tl.TextColor3 = Color3.new(1, 1, 0); tl.Size = UDim2.new(1, -20, 0, 25); tl.Position = UDim2.new(0, 10, 0, 8); tl.BackgroundTransparency = 1; tl.Font = "GothamBold"; tl.TextSize = 14; tl.TextXAlignment = "Left"

    local cl = Instance.new("TextLabel", mf)
    cl.Text = text; cl.TextColor3 = Color3.new(1, 1, 1); cl.Size = UDim2.new(1, -20, 0, 35); cl.Position = UDim2.new(0, 10, 0, 32); cl.BackgroundTransparency = 1; cl.Font = "Gotham"; cl.TextSize = 13; cl.TextXAlignment = "Left"; cl.TextWrapped = true

    mf:TweenPosition(UDim2.new(1, -270, 1, -100), "Out", "Back", 0.5, true)
    task.delay(duration or 5, function()
        if mf then mf:TweenPosition(UDim2.new(1, 10, 1, -100), "In", "Quad", 0.5, true, function() sg:Destroy() end) end
    end)
end

-- [[ GIAO DIỆN CHÍNH ]]
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
ScreenGui.Name = "VuKhoa_Pro_Squircle"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.Position = UDim2.new(0.5, -115, 0.2, 0)
MainFrame.Size = UDim2.new(0, 230, 0, 420) -- Kéo dài ra một chút để chứa đủ nút
MainFrame.ClipsDescendants = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

-- Title Bar (Màu đen hoàn toàn)
local TitleBar = Instance.new("Frame", MainFrame)
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
TitleBar.BorderSizePixel = 0

local ToggleBtn = Instance.new("TextButton", TitleBar)
ToggleBtn.Text = "  🔻 TRVŨKHOAA PRO"
ToggleBtn.Size = UDim2.new(1, -40, 1, 0)
ToggleBtn.BackgroundTransparency = 1
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = "GothamBold"; ToggleBtn.TextSize = 13; ToggleBtn.TextXAlignment = "Left"

local CloseBtn = Instance.new("TextButton", TitleBar)
CloseBtn.Text = "✕"
CloseBtn.Size = UDim2.new(0, 40, 1, 0)
CloseBtn.Position = UDim2.new(1, -40, 0, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = "GothamBold"; CloseBtn.TextSize = 16

-- Floating Icon
local FloatingBtn = Instance.new("ImageButton", ScreenGui)
FloatingBtn.Size = UDim2.new(0, 55, 0, 55)
FloatingBtn.Position = UDim2.new(0, 20, 0, 20)
FloatingBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
FloatingBtn.Image = "rbxassetid://6031171742"
FloatingBtn.Visible = false
Instance.new("UICorner", FloatingBtn).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", FloatingBtn).Color = Color3.new(1,1,1)

-- [[ HÀM KÉO THẢ (FIXED) ]]
local function makeDraggable(window, handle)
    local dragging, dragInput, dragStart, startPos
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position; startPos = window.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    uis.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            window.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
makeDraggable(MainFrame, TitleBar)
makeDraggable(FloatingBtn, FloatingBtn)

-- Logic Thu Nhỏ / Mở Rộng
local isOpen = true
ToggleBtn.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    MainFrame:TweenSize(isOpen and UDim2.new(0, 230, 0, 420) or UDim2.new(0, 230, 0, 40), "Out", "Quad", 0.3, true)
    ToggleBtn.Text = isOpen and "  🔻 TRVŨKHOAA PRO" or "  ▶ TRVŨKHOAA PRO"
end)

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false; FloatingBtn.Visible = true
    FloatingBtn.Position = UDim2.new(0, MainFrame.AbsolutePosition.X, 0, MainFrame.AbsolutePosition.Y)
end)
FloatingBtn.MouseButton1Click:Connect(function() FloatingBtn.Visible = false; MainFrame.Visible = true end)

-- [[ KHU VỰC CHỨC NĂNG ]]
local Content = Instance.new("ScrollingFrame", MainFrame)
Content.Size = UDim2.new(1, 0, 1, -85); Content.Position = UDim2.new(0, 0, 0, 45)
Content.BackgroundTransparency = 1; Content.ScrollBarThickness = 0; Content.CanvasSize = UDim2.new(0,0,0,360)
local ContentLayout = Instance.new("UIListLayout", Content); ContentLayout.Padding = UDim.new(0, 5); ContentLayout.HorizontalAlignment = "Center"

-- Khung Grid (Lưới di chuyển)
local GridFrame = Instance.new("Frame", Content)
GridFrame.Size = UDim2.new(0.92, 0, 0, 115); GridFrame.BackgroundTransparency = 1
local UIGrid = Instance.new("UIGridLayout", GridFrame); UIGrid.CellSize = UDim2.new(0.48, 0, 0, 35); UIGrid.CellPadding = UDim2.new(0.04, 0, 0, 5)

-- Khung List (Các chức năng Bật/Tắt)
local ListFrame = Instance.new("Frame", Content)
ListFrame.Size = UDim2.new(1, 0, 0, 180); ListFrame.BackgroundTransparency = 1
local UIList = Instance.new("UIListLayout", ListFrame); UIList.Padding = UDim.new(0, 5); UIList.HorizontalAlignment = "Center"

-- Thanh Speed ở dưới cùng
local SpeedBar = Instance.new("Frame", MainFrame)
SpeedBar.Size = UDim2.new(1, 0, 0, 40); SpeedBar.Position = UDim2.new(0, 0, 1, -40)
SpeedBar.BackgroundColor3 = Color3.fromRGB(15, 15, 15); SpeedBar.BorderSizePixel = 0

local TocLbl = Instance.new("TextLabel", SpeedBar)
TocLbl.Text = "  Tốc độ:"; TocLbl.Size = UDim2.new(0, 80, 1, 0); TocLbl.BackgroundTransparency = 1
TocLbl.Font = "GothamBold"; TocLbl.TextColor3 = Color3.new(1, 1, 1); TocLbl.TextSize = 13; TocLbl.TextXAlignment = "Left"

local SpeedVal = Instance.new("TextBox", SpeedBar)
SpeedVal.Text = "1"; SpeedVal.Size = UDim2.new(0, 50, 0, 24); SpeedVal.Position = UDim2.new(0, 80, 0.5, -12)
SpeedVal.BackgroundColor3 = Color3.fromRGB(30, 30, 30); SpeedVal.TextColor3 = Color3.new(1, 1, 1)
SpeedVal.Font = "GothamBold"; SpeedVal.TextSize = 13
Instance.new("UICorner", SpeedVal).CornerRadius = UDim.new(0, 4)

SpeedVal.FocusLost:Connect(function() 
    currentSpeed = tonumber(SpeedVal.Text) or 1 
    CreateNotify("Hệ Thống", "Đã chỉnh tốc độ thành: " .. currentSpeed, 2)
end)

-- [[ HÀM TẠO NÚT CHUNG ]]
local function createBtn(parent, txt, callback)
    local btn = Instance.new("TextButton", parent)
    if parent == ListFrame then btn.Size = UDim2.new(0.92, 0, 0, 38) end
    btn.BackgroundTransparency = 1; btn.Text = ""
    
    local bg = Instance.new("Frame", btn)
    bg.Size = UDim2.new(1, 0, 1, 0); bg.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 6)
    
    local box = Instance.new("Frame", btn)
    box.Size = UDim2.new(0, 16, 0, 16); box.Position = UDim2.new(0, 10, 0.5, -8); box.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)
    
    local lbl = Instance.new("TextLabel", btn)
    lbl.Text = txt; lbl.Size = UDim2.new(1, -35, 1, 0); lbl.Position = UDim2.new(0, 35, 0, 0); lbl.BackgroundTransparency = 1; lbl.TextColor3 = Color3.new(0.8, 0.8, 0.8); lbl.Font = "Gotham"; lbl.TextSize = 13; lbl.TextXAlignment = "Left"

    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        box.BackgroundColor3 = active and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(40, 40, 40)
        lbl.TextColor3 = active and Color3.new(1, 1, 1) or Color3.new(0.8, 0.8, 0.8)
        callback(active)
    end)
end

-- TẠO NÚT DI CHUYỂN
local moves = {{"Tiến", Vector3.new(0,0,-1)}, {"Lùi", Vector3.new(0,0,1)}, {"Trái", Vector3.new(-1,0,0)}, {"Phải", Vector3.new(1,0,0)}, {"Lên", Vector3.new(0,1,0)}, {"Xuống", Vector3.new(0,-1,0)}}
for _, v in pairs(moves) do 
    createBtn(GridFrame, v[1], function(s) 
        if s then localMoveDir = localMoveDir + v[2] else localMoveDir = localMoveDir - v[2] end 
    end) 
end

-- TẠO NÚT CHỨC NĂNG KHÁC
createBtn(ListFrame, "Ghim Vị Trí", function(s) ghimActive = s; if s and lp.Character then lockedPos = lp.Character.HumanoidRootPart.CFrame end end)
createBtn(ListFrame, "Xray ESP", function(s) xrayActive = s end)
createBtn(ListFrame, "Ghost Mode", function(s)
    ghostActive = s
    if s and lp.Character then
        lp.Character.Archivable = true; ghostModel = lp.Character:Clone(); ghostModel.Parent = workspace
        for _, v in pairs(ghostModel:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false; v.Transparency = 0.5 end end
        workspace.CurrentCamera.CameraSubject = ghostModel:FindFirstChild("Humanoid")
    elseif ghostModel then ghostModel:Destroy(); workspace.CurrentCamera.CameraSubject = lp.Character.Humanoid end
end)
createBtn(ListFrame, "Air Walk", function(s)
    airWalkActive = s
    if s then airWalkPart = Instance.new("Part", workspace); airWalkPart.Size = Vector3.new(15, 0.5, 15); airWalkPart.Anchored = true; airWalkPart.Transparency = 0.5
    elseif airWalkPart then airWalkPart:Destroy() end
end)

-- [[ VÒNG LẶP XỬ LÝ - HEARTBEAT ]]
runService.Heartbeat:Connect(function(dt)
    local char = lp.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local root = char.HumanoidRootPart
    
    -- XRAY
    if xrayActive then
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= lp and p.Character and not p.Character:FindFirstChild("TrVuKhoaa_ESP") then
                local h = Instance.new("Highlight", p.Character); h.Name = "TrVuKhoaa_ESP"; h.FillColor = Color3.new(1, 0, 0)
            end
        end
    else
        for _, p in pairs(game.Players:GetPlayers()) do if p.Character and p.Character:FindFirstChild("TrVuKhoaa_ESP") then p.Character.TrVuKhoaa_ESP:Destroy() end end
    end

    -- ĐIỀU KHIỂN & NOCLIP
    local target = (ghostActive and ghostModel) and ghostModel:FindFirstChild("HumanoidRootPart") or root
    if localMoveDir.Magnitude > 0 then
        -- Xuyên tường khi di chuyển
        for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
        if ghostModel then for _, v in pairs(ghostModel:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end end
        
        local moveVec = (target.CFrame.LookVector * -localMoveDir.Z) + (target.CFrame.RightVector * localMoveDir.X) + (Vector3.new(0,1,0) * localMoveDir.Y)
        target.CFrame = target.CFrame + (moveVec.Unit * (currentSpeed * 50 * dt))
        if ghimActive and not ghostActive then lockedPos = target.CFrame end
    end
    
    -- XỬ LÝ GHIM & GHOST
    if ghimActive and lockedPos and not ghostActive then root.CFrame = lockedPos; root.Velocity = Vector3.new(0,0,0) end
    if ghostActive then root.CFrame = lockedPos or root.CFrame; root.Velocity = Vector3.new(0,0,0) end
    if airWalkActive and airWalkPart then airWalkPart.Position = target.Position - Vector3.new(0, 3.5, 0) end
end)

CreateNotify("HỆ THỐNG", "TrVũKhoaa Pro Đã Kích Hoạt Đầy Đủ Chức Năng!", 5)
